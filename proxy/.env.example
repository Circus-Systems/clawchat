# ──────────────────────────────────────────────
# ClawChat Proxy Configuration
# ──────────────────────────────────────────────

# REQUIRED: Token the browser uses to authenticate to this proxy.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CLAWCHAT_UI_TOKEN=

# REQUIRED: Token the proxy uses to authenticate to the OpenClaw Gateway.
# Find with: openclaw config get gateway.auth.token
OPENCLAW_GATEWAY_TOKEN=

# Gateway WebSocket URL (default: local loopback)
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789

# OpenClaw home directory (default: ~/.openclaw)
# OPENCLAW_HOME=

# Proxy server binding
HOST=0.0.0.0
PORT=3100

# CORS allowed origins (comma-separated). Leave blank to auto-allow all.
# CORS_ORIGINS=http://100.x.x.x:3100,https://mac-studio.tail12345.ts.net
